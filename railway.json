{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "php bootstrap/railway-setup.php && php artisan serve --host=0.0.0.0 --port=$PORT",
    "healthcheckPath": "/check.php",
    "healthcheckTimeout": 300
  }
}
